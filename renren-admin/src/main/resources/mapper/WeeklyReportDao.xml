<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.attendance.dao.WeeklyReportDao">
    <select id="selectByUserIdAndDateRange" resultType="io.renren.modules.attendance.entity.WeeklyReportEntity">
        SELECT * FROM weekly_report 
        WHERE user_id = #{userId} 
        AND start_date >= #{startDate}
        AND end_date <![CDATA[<=]]> #{endDate}
        ORDER BY create_time DESC
    </select>
    
    <select id="selectByUserIdAndDate" resultType="io.renren.modules.attendance.entity.WeeklyReportEntity">
        SELECT * FROM weekly_report 
        WHERE user_id = #{userId} 
        AND start_date >= #{startDate}
        AND end_date <![CDATA[<=]]> #{endDate}
    </select>
</mapper>