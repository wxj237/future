<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.renren.modules.sys.dao.InboxDao">

    <select id="getUserInbox" resultType="io.renren.modules.sys.entity.InboxEntity">
        SELECT * FROM sys_inbox
        WHERE receiver_ids LIKE CONCAT('%', #{userId}, '%')
        ORDER BY create_time DESC
    </select>

    <select id="getUnreadCount" resultType="int">
        SELECT COUNT(*) FROM sys_inbox
        WHERE receiver_ids LIKE CONCAT('%', #{userId}, '%')
        AND read_status = 0
    </select>

</mapper>